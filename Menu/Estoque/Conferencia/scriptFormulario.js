/*const formulario = document.querySelector('#formulario');
const form_item_input = document.querySelectorAll('.form-item-input');

formulario.addEventListener('click', (event) => {
    removeClassItemSelected();
    event.target.classList.add('form-item-selected');
});

function removeClassItemSelected(){
    for (let index = 0; index < formulario.length; index++) {
        formulario[index].classList.remove('form-item-selected');
    }
}*/